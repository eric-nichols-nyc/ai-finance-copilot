# Test Environment Variables
# Copy this file to .env.test or .env.local and fill in your test credentials

# ==============================================================================
# Supabase Configuration (for E2E tests)
# ==============================================================================
# Your Supabase project URL and anonymous key
# These are the same as your regular Supabase credentials
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# ==============================================================================
# Test User Credentials (for Playwright E2E tests)
# ==============================================================================
# IMPORTANT: This user must exist in your Supabase Authentication
# Create it manually via:
# - Supabase Dashboard → Authentication → Users → Add User
# - Email: test@example.com (or your preferred test email)
# - Password: testpassword123 (or your preferred test password)
# - Confirm email: Yes (or tests will fail)

TEST_USER_EMAIL=test@example.com
TEST_USER_PASSWORD=testpassword123

# ==============================================================================
# Notes
# ==============================================================================
# 1. Never commit .env.test or .env.local to version control
# 2. The test user must have a confirmed email in Supabase
# 3. Use different credentials for development and testing
# 4. Consider creating a dedicated test Supabase project for CI/CD
# 5. For CI/CD, add these variables to your GitHub Secrets or CI provider
